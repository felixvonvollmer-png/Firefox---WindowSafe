# WindowSafe Project Foundation Agent Start Package

This package authorizes and routes the first V6 Coding-Agent run for **Project Foundation bootstrap only**. It does not contain or authorize normal product-feature implementation.

Start with `WindowSafe_Project_Foundation_Agent_Start_Handoff_WS-PFBOOT-20260917-01.md`, verify `SHA256SUMS.json`, then execute according to the separate Execution Authorization and exact V6 Foundation-2 blob.

The target repository was observed as public and empty immediately before package creation. The Coding Agent must recheck that baseline before any mutation and stop on mismatch.
